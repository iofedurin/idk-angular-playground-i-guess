<!--
  Инструкция для Claude:
  1. Определи свою модель. Opus = claude-opus-*, Sonnet = claude-sonnet-*. Проверь model ID в system prompt.
  2. Работай ТОЛЬКО со своей секцией (TODO для Sonnet / TODO для Opus).
  3. Не трогай чужую секцию, кроме случая, когда передаёшь задачу другой модели.
  4. После выполнения задачи — удали её из списка. Заголовки секций не удалять.
-->

TODO для Sonnet:

TODO для Opus:
- добавить какой-то бэкграунд для канваса, чтоб было прозрачней, что как двигается (сетка может, хз)
- с каждой итерацией изменений компонент страницы орг борд становится всё толще. Ты архитектор, подумай как крисиво отрефакторить. Может быть сервисы, может еще какая-то декомпозиция на компоненты (с content-projection).
- Спланировать и реализовать сохранение connection mode preference в localStorage (org-board canvas: step vs bezier toggle)
- как добавить какой-нибудь опенсорс пак иконок.
    1. сделать выбор иконки для департамента
    2. показывать иконку департамента у пользователя на канвасе
- подумать как переделать в тестировании след кусок кода. Везде приходится добавлять. Не обязательно, но если получится - замечательно
// Foblex uses ResizeObserver internally (FResizeChannel) — jsdom doesn't include it
(window as any)['ResizeObserver'] = class {
  observe() {}
  unobserve() {}
  disconnect() {}
};
- запланировать большую доработку, что можно создавать на доске команды и указывать в качестве таргета связи команду (выделяется на доске рамкой для выбранных блоков, можно вместе перемещать, можно вместе удалять)