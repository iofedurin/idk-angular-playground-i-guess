<!-- Инструкция: Claude определяет свою модель (Opus / Sonnet) и работает ТОЛЬКО со своей секцией. -->

TODO для Sonnet:
1. Закомитить этот файл без конкретных тудушек, с заголовками. Потом добавить в гитигнор, чтоб оно ниче не коммитило сюда. План:
   - Сохранить текущее содержимое файла (запомнить/скопировать)
   - `git stash` или сбросить файл до состояния "только заголовки" (`git checkout -- docs/user-todo.md` не подойдёт — файл ещё не закоммичен с заголовками)
   - Записать в файл только два заголовка (без тудушек), закоммитить: `git add docs/user-todo.md && git commit -m "docs: add user-todo.md with section headers"`
   - Добавить `docs/user-todo.md` в `.gitignore`, закоммитить: `git add .gitignore && git commit -m "chore: gitignore user-todo.md"`
   - Вернуть файлу сохранённое содержимое (восстановить все тудушки)
2. Bulk-действия на fake-backend — есть план `docs/PLAN-bulk-backend.md`, реализовать по нему
3. Добавить в CLAUDE.md инструкцию, что скилы добавляются через сабмодули. План:
   - Переименовать секцию "Updating Skills" → "Skills (submodules)"
   - Добавить пояснение: `.claude/angular-skills/` — git submodule, не копировать файлы вручную
   - Добавить команду добавления нового skill-репо: `git submodule add <repo-url> .claude/<skill-name>`

TODO для Opus:
1. Хочется понять, по какой логике ты декомпозировал страницу дашборда. Большая часть верстки там прямо в самом компоненте, но app-stats ты почему-то вынес в отдельный виджет.
2. Решение с ресетом всех сторов не буллетпруф, хочется лучше. Потому что это на разрабочике, вспомнить, что надо сделать метод ресет и вызвать его потом в этом гварде. Как можем решить более элегантно смену аппов?
